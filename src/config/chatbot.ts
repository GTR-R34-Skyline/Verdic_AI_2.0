/**
 * Chatbot Configuration
 * Toggle the chatbot embed feature on/off
 */
export const chatbotConfig = {
  /**
   * Enable or disable the floating chatbot widget globally
   * Set to false to disable the chatbot across all pages
   */
  enabled: true,
  
  /**
   * Enable chatbot on homepage
   */
  enabledOnHomepage: true,
  
  /**
   * Enable chatbot on case detail pages
   */
  enabledOnCaseDetail: true,
};

export default chatbotConfig;
